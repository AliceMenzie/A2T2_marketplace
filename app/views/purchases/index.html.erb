<div class="center jumbotron">

<h1 class="text-center">My Listings <%= current_user.username %></h1> <br/>  

</div>

<div class="container">
  <div class="row"> 
      <div class="card-body" >
<% @purchases.each do |purchase| %>
<div class="col">
<div class="card mb-5 mt-3" style="width: 30rem;" class="well block margin-bottom-six">

<h4 class="text-center">Bought on:<%= purchase.id %> </h4>


<%= purchase.user_id %>
<p>Product Bought:<%= purchase.listing.name %> </p>

<p>Purchased From:</p>
<%= purchase.user.username %>

<%= link_to "Reciept", purchase.receipt_url %>
</div>
</div>
<% end %>
</div>
</div>
</div>
