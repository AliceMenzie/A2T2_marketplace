

 <% @listings.each do |listing| %>
  <ul>
  <li><%=listing.name %></li>
  <li><%=listing.description %></li>
	<li><%=listing.category %></li>
  <li><%=number_to_currency(listing.price) %></li>
  <li><%= link_to 'View Product', listing_path(listing) %> 
  </li>
  </ul>
 <% end %>


<%= link_to 'Create New Product', new_listing_path  %>